version: 1
applications:
  - appRoot: app
    frontend:
      phases:
        preBuild:
          commands:
            - echo "Static panel – no build step"
        build:
          commands:
            - echo "Copying as-is"
      artifacts:
        baseDirectory: .
        files:
          - '**/*'
      cache:
        paths: []
